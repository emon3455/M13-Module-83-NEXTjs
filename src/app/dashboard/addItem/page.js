import React from 'react';

export const metadata = {
    title: 'Add Item | Dashboard',
    description: 'First NextJS APP',
}

const AddItem = () => {
    return (
        <div>
            add item page
        </div>
    );
};

export default AddItem;