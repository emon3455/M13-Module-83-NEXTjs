import React from 'react';

export const metadata = {
    title: 'About Page',
    description: 'First NextJS APP',
}

const AboutPage = () => {
    return (
        <div>
            this is about page
        </div>
    );
};

export default AboutPage;